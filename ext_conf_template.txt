# cat=reload; type=string; label=Comma-separated application contexts in which the extension is active; an entry matches itself and its subcontexts; a bare "Production" entry is ignored; empty = disabled everywhere
activeContexts = Development

# cat=reload; type=options[tagged,always]; label=Reload mode: tagged = only tabs whose cache tags intersect the broadcast, always = every connected tab
reloadMode = tagged

# cat=reload; type=boolean; label=Reload on Fluid template and ViewHelper file changes (Development context, Vite transport); disables the frontend page cache in Development so every render reflects the current files
fileReload = 1

# cat=reload; type=int+; label=Poll interval in milliseconds for the editor reload transport; minimum 1000
pollInterval = 3000

# cat=reload; type=int+; label=Seconds a broadcast stays available to polling clients; minimum 60
retention = 300

# cat=reload; type=string; label=Vite dev server URL reachable from PHP (broadcast target)
viteServerInternalUrl = http://localhost:5173

# cat=reload; type=string; label=Vite dev server URL reachable from the browser; empty = resolve via vite-asset-collector when installed
viteServerPublicUrl =

# cat=reload; type=string; label=Shared secret sent to the Vite dev server with each broadcast; configure the same value in the vite plugin ("secret" option) to reject unauthenticated posts; empty = no authentication
viteSharedSecret =
